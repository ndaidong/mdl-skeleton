/**
 * App init
 * @ndaidong
 */

import {
  add
} from './utils.js';

console.log('Call imported method:');
console.log(add(3, 5));

// refer to shared data
console.log('Shared data:');
console.log(SDATA);
